apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: llm
spec:
  type: conversation.openai
  version: v1
  metadata:
  - name: model
    value: "gpt-4o-mini"
  - name: key
    secretKeyRef:
      name: OPENAI_API_KEY
      key: OPENAI_API_KEY
auth:
  secretStore: envvar-secrets
